// other options are not added due to they are not used in tests in this demo
export enum Industry {
    Medical = 'Medical',
    InsuranceAndBenefits = 'Insurance & Benefits' // same values are used for UI and API for now
}